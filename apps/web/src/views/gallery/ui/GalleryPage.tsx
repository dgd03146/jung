'use client';

import { PhotoTabs } from '@/fsd/features/gallery/photos';
import { Container } from '@jung/design-system/components';

const GalleryPage = () => {
	return (
		<Container>
			<PhotoTabs />
		</Container>
	);
};

export default GalleryPage;
