import { style } from '@vanilla-extract/css';

export const borderBottomStyle = style({
	borderBottomWidth: '1px',
});

export const growthChartContainer = style({
	flex: 3,
});

export const distributionChartContainer = style({
	flex: 2,
});

export const legendText = style({
	color: '#4B5563',
	fontSize: '0.875rem',
	fontWeight: '500',
});
