# 복잡한 조건에 이름 붙이기

## 가독성
복잡한 조건식이 특별한 이름 없이 사용되면, 조건이 뜻하는 바를 한눈에 파악하기 어려워요.

---

## 📝 코드 예시

다음 코드는 상품 중에서 **카테고리와 가격 범위가 일치하는 상품만 필터링**하는 로직이에요.

```ts
const result = products.filter((product) =>
  product.categories.some(
    (category) =>
      category.id === targetCategory.id &&
      product.prices.some((price) => price >= minPrice && price <= maxPrice)
  )
);


⸻

👃 코드 냄새 맡아보기

가독성

이 코드에서는 익명 함수와 조건이 복잡하게 얽혀 있어요.
filter, some, && 같은 로직이 여러 단계로 중첩되어 있어서 정확한 조건을 파악하기 어려워졌어요.

코드를 읽는 사람이 한 번에 고려해야 하는 맥락이 많아서, 가독성이 떨어져요.[^1]

⸻

✏️ 개선해보기

다음 코드와 같이 조건에 명시적인 이름을 붙이면,
코드를 읽는 사람이 한 번에 고려해야 할 맥락을 줄일 수 있어요.

const matchedProducts = products.filter((product) => {
  return product.categories.some((category) => {
    const isSameCategory = category.id === targetCategory.id;
    const isPriceInRange = product.prices.some(
      (price) => price >= minPrice && price <= maxPrice
    );

    return isSameCategory && isPriceInRange;
  });
});

명시적으로
	•	같은 카테고리 안에 속해 있고
	•	가격 범위가 맞는 제품들

로 필터링한다고 작성함으로써, 복잡한 조건식을 따라가지 않고도 코드의 의도를 명확히 드러낼 수 있어요.

⸻

🔍 더 알아보기: 조건식에 이름을 붙이는 기준

언제 조건식이나 함수에 이름을 붙이고 분리하는 것이 좋을까요?

⸻

조건에 이름을 붙이는 것이 좋을 때

1. 복잡한 로직을 다룰 때
조건문이나 함수에서 복잡한 로직이 여러 줄에 걸쳐 처리되면,
이름을 붙여 함수의 역할을 명확히 드러내는 것이 좋아요.
	•	코드 가독성 향상
	•	유지보수 용이
	•	코드 리뷰가 쉬워짐

⸻

2. 재사용성이 필요할 때
동일한 로직을 여러 곳에서 반복적으로 사용할 가능성이 있으면,
변수나 함수를 선언해 재사용할 수 있어요.
	•	코드 중복 감소
	•	유지보수 비용 감소

⸻

3. 단위 테스트가 필요할 때
함수를 분리하면 독립적으로 단위 테스트를 작성할 수 있어요.
	•	함수가 올바르게 동작하는지 쉽게 검증 가능
	•	복잡한 로직을 테스트할 때 특히 유용

⸻

조건에 이름을 붙이지 않아도 괜찮을 때

1. 로직이 간단할 때
로직이 매우 간단하면 굳이 이름을 붙이지 않아도 돼요.

arr.map(x => x * 2)


⸻

2. 한 번만 사용될 때
특정 로직이 코드 내에서 한 번만 사용되고,
그 로직이 복잡하지 않으면 익명 함수에서 직접 로직을 처리하는 것이 더 직관적일 수 있어요.

⸻

🧠 참고

프로그래머의 뇌에 따르면,
사람의 뇌가 한 번에 저장할 수 있는 정보의 숫자는 약 6개라고 해요.[^1]

⸻

✨ 표현 개선 아이디어

isPriceInRange와 같이 범위를 검사하는 로직은
수학에서 작성하는 것처럼 쓰는 것이 읽기 좋을 수 있어요.

// minPrice ≤ price ≤ maxPrice
(price) => minPrice <= price && price <= maxPrice


⸻

🔧 로직만 분리해보기 (실행 구조는 유지)

실행 흐름은 유지하되, 로직만 분리해 가독성을 높일 수 있어요.
또한 기존 코드에서 카테고리를 순회하면서 가격을 함께 체크하는 구조는 다소 어색해 보일 수 있어요.

const isSameCategory = (a: Category, b: Category) => a.id === b.id;

const hasTargetCategory = (
  categories: Category[],
  targetCategory: Category
) => categories.some((category) => isSameCategory(category, targetCategory));

const isPriceInRange = (
  price: number,
  minPrice: number,
  maxPrice: number
) => price >= minPrice && price <= maxPrice;

const hasPriceInRange = (
  prices: number[],
  minPrice: number,
  maxPrice: number
) => prices.some((price) => isPriceInRange(price, minPrice, maxPrice));

const matchedProducts = products.filter((product) => {
  return (
    hasTargetCategory(product.categories, targetCategory) &&
    hasPriceInRange(product.prices, minPrice, maxPrice)
  );
});


⸻

🔄 순회 구조를 명확히 분리한 예시

some 루프를 분리하면 가독성과 확장성이 더 좋아질 수 있어요.

const matchedProducts = products.filter(product => {
  // 1) 먼저 카테고리 일치 여부만 검사
  const hasCategory = product.categories.some(
    c => c.id === targetCategory.id
  );
  if (!hasCategory) return false;

  // 2) 일치하는 제품에 대해서만 가격 범위 검사
  const hasRightPrice = product.prices.some(
    p => p >= minPrice && p <= maxPrice
  );
  return hasRightPrice;

  // 3) ... 이후에 추가되는 검사 로직
});


⸻

✅ 정리

Huinno-joonhonoh님께서 말씀해주신 것처럼
some 루프를 분리하면 가독성과 추후 확장성이 더 좋아질 수 있어요.
	•	조건의 의미가 더 명확해지고
	•	로직 추가/수정이 쉬워지며
	•	코드 리뷰 시 의도를 빠르게 파악할 수 있어요.

⸻


